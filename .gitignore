__pycache__/
*.pyc
console/data/
# Back-upkopieën van de databases: horen op de droplet, niet in de repo.
backups/
.env
certs/

# Restanten van de verwijderde Streamlit-app. De map zit niet meer in de
# repo, maar staat op bestaande droplets nog wél op schijf (database +
# OAuth-tokencache). Blijft genegeerd zodat die er nooit alsnog per ongeluk
# in gecommit worden.
data/
