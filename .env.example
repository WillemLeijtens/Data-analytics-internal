STREAMLIT_APP_PASSWORD=change-me
# Must be the droplet's actual public IP (or a domain, once you have one) —
# Caddy needs this to know what to put in the TLS certificate's SAN.
SITE_ADDRESS=change-me
